<section id="disconnections" class="section-grey">
    <div class="container">
        <div class="row">
            <div class="col-lg-6">

            </div>
            <div class="col-lg-6">
                <div class="section-title">
                    <h3>{% trans %}Плановые отключения{% endtrans %}</h3>
                </div>
                <ul class="list-unstyled">
                    {% for post in posts %}
                        <li><a href="{{ url('post_show', {id: post.id}) }}"><h4>{{ post.title }}</h4></a></li>
                    {% endfor %}
                </ul>
            </div>
        </div>
    </div>
</section>